# Progress

- Slice 1 (2026-08-27): project skeleton — pyproject, the 8 section-3 entities as dataclasses on SQLite behind a plain-sqlite3 repository layer, append-only AuditLog, isolated `llm/` boundary (protocol, fake + stubbed openai providers, factory, TokenMeter, versioned prompts), hardcoded fixtures and console report in `demo/`, and one end-to-end path in `main.py`. 31 tests passing.
- Slice 2 (2026-08-27): seeded synthetic corpus — 7 process areas, 14 clause-structured controls (3 structured), 12-month calendar, 316 evidence submissions across all seven qualities incl. near-misses, a two-area consistency pair, 3 exceptions (one lapsing mid-year), 6 remediations, and a truth file outside `src/` that nothing in the package may read. 125 tests passing.
- Slice 3 (2026-08-28): S0 applicability rules engine — deterministic `applies_when` evaluation (equality, any-of list, empty = everywhere) producing the control set per area with a per-condition explanation, unknown-attribute validation, one audit event per area, and zero model calls asserted with every provider rigged to explode. Reproduces all 64 corpus pairings. 154 tests passing.
- Slice 4 (2026-08-28): S1 scheduling and trigger — `run_cycle(as_of)` as the single entry point (cron, UI and CLI share it) with `advance_calendar` delegating to it, 343 instances generated across the 12-month calendar keyed on control x area x period, pending/submitted/overdue/waived/assessed state machine, escalation up a 3-level owner chain read back out of the audit trail, notifications logged and never sent, exception expiry alerts with lapse-stable suppression, human-vs-system trigger recorded. Period arithmetic moved from synth/ to `periods.py` so the engine does not import test data. 201 tests passing.
- Slice 4a (2026-08-28): added EXC-004, a fourth exception granted 2026-05-11 after CTRL-CRYPTO-KEY/AREA-HR 2026-Q1 was already open and overdue, making the `waived` transition reachable from the corpus. Introduced `waives()` in S1 as the complement of `covers()`: suppression settles at generation, waiver settles every cycle. Its window contains no period end, so the corpus fingerprint is unchanged at 8ef5f63c47512bdc. 213 tests passing.
- Slice 5 (2026-08-28): S2 pre-screen — five ordered rules (no evidence, wrong type, unchanged content hash carried forward, stale by freshness, structured threshold evaluated in code) resolving 140 of 342 instances (40.9%) with zero model calls; only 202 ambiguous document instances survive to S3. Evidence made write-once at both layers: `WriteOnceRepository` has no update/delete and SQLite triggers refuse UPDATE and DELETE. Findings gain `decided_by` and `carried_forward_from`. 248 tests passing.
- Slice 6 (2026-08-28): S3 assessment — paragraph chunking with deterministic term-overlap retrieval, one metered model call per surviving instance (202 of 342), strict JSON with enum-validated verdict, citations verified against the source or the assessment fails to insufficient_evidence + human review, untrusted-evidence delimiters with an adversarial document in the corpus, provenance (criteria hash, prompt version, evidence hash) on every Finding, retry-once then fail cleanly, confidence floor 0.6. Real OpenAI provider implemented, key from .env via env var. 294 tests passing.
- Slice 7 (2026-08-28): S4 flagging and the resolution loop — three distinct categories in a `Flag` table (121 gap, 32 overdue, 4 exception), severity stamped from a documented four-term formula with its arithmetic in the rationale, 153 Actions raised and routed to owning teams with SLA by severity band, Actions escalating on their own timer separate from the check, and `reassess(check_instance_id)` closing the loop on demand: remediation bound as new write-once evidence, re-run through S2/S3, new Finding with supersedes_finding_id, Action resolved with a note. All 6 corpus remediations close. 343 tests passing.
- Slice 7b (2026-09-02): audit log made tamper-evident — every AuditEvent carries a monotonic `seq`, the `prev_hash` of the entry before it, and its own `entry_hash` covering that link. `verify_chain()` walks the log and returns OK or the sequence number of the first broken link, distinguishing an altered entry, a broken predecessor link and a non-contiguous sequence. 2397 entries verify clean over a full year; one owner rewritten by raw UPDATE at seq 1500 is caught with 1499 entries verified before it. No other behaviour changed. 360 tests passing.
- Slice 8 (2026-09-02): evaluation harness in a top-level `evaluation/` package (outside `src/`, so the truth-isolation guarantee is structural) — competent naive baseline that skips empty instances and caches by document hash but has no applicability, pre-screen or retrieval, disk-cached and never recomputed; a documented simulated-manual model with a sensitivity table; all seven metrics; `results.md` written with every scope qualifier in the file. Pipeline run month by month so time-to-detection is real. 389 tests passing.
- Slice 8b (2026-09-02): Audit Evidence Pack generator (`pack.py`) built from the audit log alone — cover, coverage, exception register, findings register with cited excerpts inline, action register, full chronological trail, method note and chain verification, rendered to markdown and self-contained HTML. Enforced two ways: a static check that no live-state table is queried and no repository imported, and a runtime test building a complete pack from a database containing only the audit log. Building it exposed three holes in the trail, all fixed at source: findings stored a citation count not the text, exceptions that never lapsed produced no event, and instances recorded ids but no titles. 413 tests passing.
- Slice 8c (2026-09-02): fixed two defects the audit pack exposed — a waiver granted after S2 had already closed a check as failed now excuses it (instance to waived, gap/overdue flag closed, action resolved with a note, finding left standing on the record), and audit entries are stamped from a simulated clock scoped to each stage entry point so the trail carries cycle dates rather than the wall clock of a machine replaying a year in seconds. 422 tests passing.
- Slice 9 (2026-09-02): Streamlit dashboard — one screen with status by area, overdue/escalation queue, finding detail with cited spans highlighted inside the source document, open actions, per-check audit timeline, live token and cost meter, calendar advance, run-cycle, re-assess, evidence upload, audit-pack generation and chain verification. Logic split into `view.py` and `service.py` (neither imports Streamlit) with `app.py` as layout; headless AppTest runs the real script and clicks the buttons. 458 tests passing.
- Slice 9a (2026-09-02): dashboard usability — guided six-step walkthrough in plain English driving the same service functions as the console, evidence uploads and re-assessment now report back on screen (messages parked in session state, since anything drawn before `st.rerun()` is discarded), long documents rendered in a fixed-height scrolling iframe that opens on the first citation, and markdown emphasis converted for styled panels. Plus `docs/USER_GUIDE.md` with 13 screenshots captured from the running app by `docs/capture.py`. 488 tests passing.
- Slice 10a (2026-09-02): v3 vocabulary — `ProcessArea` -> `AuditableUnit` with a `kind`, new `Identity` with roles and reporting lines, `AuditEvent.actor` -> `actor_kind` + `actor_identity`, and the v2 `Finding` (an AI verdict) -> `Assessment`, which vacates the name before a later slice reuses it for the central tracked object. Escalation walks `reports_to` through a new `directory` rather than gluing "Head of" onto a team name. Unit roster deliberately unchanged (7 units, 64 pairings, 343 instances) so every moved test is a rename, not a recount. 488 tests passing, chain verifies clean over 2,212 entries.
- Slice 10b (2026-09-07): the v3 `Finding` becomes the central tracked object. `Action` and its seven-state machine are gone; a finding is **Open or Closed** and only a `pa_infosec` identity closes one, with remarks required. S4 raises the finding and stops — the computed severity band becomes a *suggestion* and the auditor's assignment is recorded beside it, so an override stays visible. New `stages/followup.py` runs the two section-5 clocks on every open finding: reminders and escalation, both severity-driven, escalating up `reports_to` *and* to PA/InfoSec, with a test asserting no row in the table lets anything drift more than a week past target and another asserting reminders continue after escalation. `follow_up_count` increments on every reminder and every insufficient-evidence round; owner progress is self-reported and closes nothing. Remediation now records progress, then either closes the finding or counts the failed round and leaves it open. The audit pack's action register becomes a finding register reconstructed from the trail — reminders, escalations and rounds included. Two defects found on the way: level-1 escalation and the follow-up stage writing wall-clock timestamps, both fixed and both now tested.
- Slice 10c (2026-09-07): section 4's review loop and section 7's segregation of duties. `EvidenceSubmission` vacated to `InboundSubmission` (the pre-binding inbox it always was) and reused for what section 3 says it is: one review round on one finding, with `auditor_response`, never edited once answered — revised evidence opens round N+1. New `authority.py` holds two separate rules, because conflating them is how segregation of duties quietly stops working: the role table, and the rule a table cannot express, that whoever submitted evidence may never accept it or close the finding. Both blocked paths tested, and both guards verified by removing them and watching exactly five tests fail. UI gained an identity selector; the owner's Close control is absent, not disabled.
- Slice 10d (2026-09-07): the audit track. `ScheduledAudit` with the four kinds the stakeholder named, findings raised under an audit carrying `audit_id` and `source="audit"`, and a test asserting every finding has exactly one parent — never both, never neither. The section 6 report is deterministic except one paragraph, and every statement in that paragraph must cite finding ids, verified rather than trusted: a summary citing a finding the audit never raised is withheld and the report issues without it. The "only S3 may reach a model" test caught `audits.py` immediately; the allow-list was widened to two named stages with the reason recorded, plus a test asserting it is exactly those two.
- Slice 10f-corpus (2026-09-07): the section 10 reshape. Eighteen months (2026-01 to 2027-06, vantage point 2027-09-30), eleven units — six support functions, three project teams, two departments — and a corpus that produces **83 findings with 17 open at today**, against 214 and 211 before. The old quality weights had half of everything failing, which is not a programme in trouble but a corpus built to make a detector look good; the new ones fail about one in six. Multi-round remediations plant fixes that do not work first time, so eight findings take two rounds and seven take three and `follow_up_count` measures something real. Eight audits across all four kinds with seventeen auditor-raised findings, and two recurring gap sets each spanning three units and three periods, both crossing the support-function / project-team line — the comparison a functional org chart makes hardest to see, and the thing slice 10e's recurrence detector needs to have something to find. Seeding deliberately does not generate audit reports: drafting is the audit track's one paid step and a fixture that runs hundreds of times must not carry a bill. Three real defects surfaced during the repair rather than count updates: the audit pack showed no severity at all for audit-raised findings because it only read the model's suggestion; `periods_for(freq, year)` silently found nothing once the corpus crossed a year boundary, now fixed by `period_from_label`; and gap detection was scored on the *current* verdict, so every successfully remediated gap counted as a missed one. A fourth was the most instructive — the truth file on disk went stale mid-reshape and the evaluation scored a fresh corpus against old ground truth, reporting 12.8% recall for a pipeline that had caught almost everything. `evaluation.harness` now refuses to score on a fingerprint mismatch, because a plausible wrong number is worse than an obviously broken one.
- Slice 10e (2026-09-08): section 2's remaining model uses, in one `stages/intelligence.py` because they share a shape — they read *findings* rather than evidence, and none of them may change a status. **Classification** maps an auditor's free text onto a closed ten-category taxonomy; the taxonomy is ours and the module says so, because the stakeholder said no taxonomy exists and inventing one and then pretending it was theirs would be dishonest. A free-form label was rejected on purpose: recurrence built on it would measure the model's vocabulary rather than the organisation's problems. **Recurrence** does the elimination deterministically — same category, different unit or period, raised earlier — and asks the model only the part a rule cannot do, which is whether two descriptions written months apart by different auditors describe the same failure. Twelve of seventeen findings need no call at all. On the corpus it links privileged access left with leavers in Payments (March 2026) to contractor accounts left active in IT Services (November) to a partner API credential never revoked in Payments again (February 2027): three units, eleven months, no shared vocabulary. **The brief** is one call per cycle over a ranking the code computed, because a model that ordered the queue would be prioritising, which section 2 does not permit. Every claim in it cites finding ids or it is withheld, the same rule as the audit report. Both guards verified by removal. New `analytics.py` carries all nine of section 8's figures with a test asserting it imports no provider and a stronger one running it with `get_client` rigged to explode; the allow-list of stages that may spend is now three, named, with a companion test naming the never-AI stages so moving a decision into a spending module has to get past a test that knows the difference.
- Slice 10f (2026-09-08): the surfaces. `analytics.py` renders into the dashboard behind a **Portfolio analytics** panel and into `results.md` as a section 8 block; both say plainly that no model is involved, and the cost meter does not move when the panel opens. Two new panels carry section 2's advisory uses — recurrence links with the earlier finding quoted next to the new one, and the prioritisation brief behind a button, because one call per cycle should be a call somebody asked for. Screenshots re-captured against the live app; the capture script now polls for each button instead of sleeping a fixed 2.5 seconds and presses **Start over** first, which fixes two real defects in it: a cycle over five hundred instances takes longer than the old wait, so the shutter fired on a page that had not advanced, and the demo database persisted between runs, so a second capture photographed a walkthrough somebody had already finished.
  Two defects found while wiring the surfaces. Activity-track findings were described by the assessment's rationale, which is generated from a handful of phrasings — eighty findings shared four sentences, so anything comparing findings was comparing boilerplate and recurrence matched everything to everything. They now name the control, the period and the specific gap. And `analytics.recurring` merged links transitively, so A→B and B→C became one "set"; on this corpus that collapsed into a single set of forty-five findings across ten units, which is the union-find algorithm reporting that the graph is connected rather than anything a team can act on. It now reports links plus a per-category rollup, with the few short chains that genuinely read as one story.
- Slice 11 (2026-09-08): the corpus rebuilt at the stakeholder's own volume and in their own vocabulary. Six support functions named as they named them — IT, Admin, Purchase, HR, Finance, Facilities — plus four project teams, two auditors and two managers on reporting lines that are not uniform (Finance and Purchase report to the director, everyone else through operations, so escalation from Finance reaches a different person at level one than escalation from IT). The control library is now section 3's fourteen activities and nothing else; the six we had invented — access-export completeness, encryption key rotation, complaint-handling SLAs — were plausible and were not this organisation's, which made every applicability count a statement about a company that does not exist.
  **"Today" moved inside the window**, to 15 April 2027. That is the change with the largest visible effect: with the vantage point past the end, every obligation had already fallen due, every open finding was months overdue and the ageing buckets collapsed into a single 90+ column, which is not what a portfolio looks like on any real Tuesday. It now spreads 1 / 2 / 0 / 14 with an audit still ahead of today. 91 findings, 17 open, severity across all three, 5 findings needing three evidence rounds and 26 rounds the auditor rejected.
  **Recurrence is now scored rather than admired.** The truth file records which findings are *intended* to be the same gap, resolved to real ids, so the detector can be marked: recall 50%, precision 75% over six planted pairs. It finds the access-left-with-leavers set across IT, Admin and HR — eleven months, no shared vocabulary — and misses the supplier-files set entirely, which is exactly the case a keyword stub cannot do. Scored on the audit track only, because activity-track descriptions are generated by the system and "the same gap elsewhere" is derivable there by a rule; scored across both, precision read 1% and measured nothing.
  The stub was taught to read "3 of 5" shortfalls, because the new clause set states failures that way far more often and comparing two numbers is a mechanical job a rule can legitimately do — it was missing six near-misses instead of one. What survives is three instances of one clause, "actions arising are tracked only inside the closed tickets", which has no negation, no shortfall and no hedge: reading it as a failure requires understanding what was asked for. Measured at 96.4% agreement and recorded rather than tuned away.
- Slice 12 (2026-09-14): section 4's lifecycle, and the gate that was missing from it. Most of the model landed in 10b-10d — binary Open/Closed, advisory `owner_progress` stored separately, numbered evidence rounds with an auditor response, closure recording who/when/why, `suggested_severity` beside the auditor's own, section 7 enforced with both blocked paths tested. What was **not** enforced was section 1's "the finding remains Open until the auditor is fully satisfied. Only then is it formally closed": closure ignored the round state entirely, so a finding could be closed while the owner's evidence sat unanswered, or immediately after the auditor called it insufficient. Section 7 says *who* may close; this says *when*, and they are different rules — the right person closing at the wrong moment is exactly the failure the stakeholder described. The waiver path is the one way past it, named `excused=True` rather than special-cased silently, because "closed because the obligation was excused" and "closed because the evidence was accepted" are different facts and the trail now records which.
  New `demo/lifecycle.py` walks one finding through three rounds — insufficient, insufficient, accepted — then closure, and then six attempts the system refuses. It runs as a test, so a weakened guard stops the demo refusing and fails rather than printing a shorter list nobody counts. Building it exposed two real defects that the pipeline had been hiding: `rounds.open_round`, `rounds.respond`, `set_progress`, `record_insufficient_round` and `close_on_repo` all stamped **wall-clock** time, violating section 12, because they are normally called from inside a stage that had already entered the simulated clock — called directly, from the dashboard or a demo, the trail recorded the day the machine happened to be switched on. They take the business date now, and the demo's test asserts every row of the trail is inside the simulated window. The same functions were writing raw identity ids into the trail's `owner` column where a person's name belongs. Refusal messages were rewritten to read as sentences: "a management may not close finding" is a worse explanation than somebody stopped from doing their job deserves.
- Slice 13 (2026-09-14): notifications become records. They were dictionaries written into the audit trail, which proved the system had told somebody and was useless for doing the job — nobody could ask "what is waiting for D. Ferreira this morning?" without replaying every event and reassembling payloads. `Notification` is now a table with section 3's ten kinds, and the per-role inbox is one query against it with a different identity. Copies are **separate rows**, because "P. Kaur has read it" and "D. Ferreira has read it" are separate facts, and marking one read must not clear the other — tested. The trail still gets an entry per notification; the record is the working copy, the trail is the evidence it existed.
  PA/InfoSec are copied on the kinds section 1 says they run — findings raised, evidence arriving, anything overdue, escalations, closures, lapsed exceptions — and deliberately *not* on plain reminders, because copying the auditors on every nudge makes the copies worthless. The rule lives in one frozenset rather than at each call site. Escalation was the one place the default was wrong: that path already addresses the audit team by name, so with the automatic copy on, every auditor got the manager's copy and their own. `copy_audit=False` there, with the reason in the code, and a test asserting no inbox double-files.
  Section 5's table is now `policy_table()`, rendered from `SEVERITY_RULES` rather than restated in prose — a policy in a document drifts from the code, and "escalation after three days" becomes true in the slide deck and false in the system. The one-week ceiling is asserted, not trusted. Seven call sites in the trigger stage converge on a single `_log` bridge with `KIND_MAP` doing the vocabulary translation in one readable table; `waived` maps to `closure`, which is the nearest honest fit and says so, because inventing an eleventh kind would drift from the spec and dropping the notification would leave an owner un-told their check had been settled.
  New `demo/inbox.py` is the deliverable: one Major finding nobody answers, chased on 7, 10 and 13 February, escalating to M. Castellanos and both auditors four days past target — nine notifications, four inboxes, every stamp in simulated business time, zero model calls. Reminders continue *after* the escalation, which is the behaviour section 5 asks for and the easiest to lose. It runs as a test, and both guards were verified by planting: removing `copy_audit=False` fails two tests, and stopping reminders once a finding goes overdue fails the demo's count.
- Slice 14 (2026-09-14): section 2's four model-backed capabilities, each with the human authority over it made structural rather than promised.
  **The taxonomy is now read off the corpus.** Slice 10e wrote ten categories into the prompt package and said plainly they were ours — honest about provenance, still wrong in kind, because a taxonomy invented at a desk names the failures somebody imagined and against a different organisation every category is a near-miss. `stages/taxonomy.py` induces the set instead: batches of real finding descriptions proposed separately, then one consolidation call merging the overlap, then frozen under a content-addressed `taxonomy_version` so a rederive over an unchanged corpus lands on the same version and nothing already classified goes stale. On this corpus it reads nine categories off 27 audit findings in five calls, and two of them — `incident_follow_up_incomplete` and `backup_or_continuity_untested` — were not in the hardcoded ten, while three that were turn out to have no support in the text. **There is deliberately no fallback list**, and `classify` refuses to run without a derived taxonomy: a silent default is the exact thing this slice removes, so it is a refusal with a sentence explaining itself rather than a quiet shrug. Verified by planting a fallback and watching the refusal test fail.
  **Classification is batched** — the system prompt and the category catalogue are most of the tokens, so 27 findings cost four calls instead of 27. Batching introduces one new failure worth naming: a model that answers six of eight leaves two findings looking unclassifiable when they are only unanswered, and they would then sit uncategorised forever because `classify` only revisits findings with no category. Every id sent must come back, an answer about a finding we never sent is refused, and both have tests.
  **Severity suggestion moved to audit completion**, where section 1 puts it — "finalised after the audit completes and communicated in the report". It had been riding along with classification, which meant the suggestion existed before the audit it belonged to had finished. `classify` now writes the category only for audit-raised findings and `audits.suggest_severities` produces the suggestion beside the auditor's own assignment at the moment the two are actually compared. Activity-track findings have no audit to wait for and keep the suggestion at classification. `severity` and `suggested_severity` stay separate columns so divergence is visible: on AUD-IA-2026-H1 the model agrees four times out of five and differs on the fifth, which is the row worth showing.
  **Evidence evaluation** is `stages/review.py`, separate from `assess.py` because they judge different things — criteria we wrote versus an auditor's own sentences — and kept out of `rounds.py` for a stronger reason: `rounds.py` is where a round is *answered*, and the module that may spend must never be the module that may decide. Strict JSON with verdict, confidence, rationale, cited_spans, gaps and needs_human_review; every span checked against the submitted evidence character for character, whitespace folded and nothing else. A span that is not there means the model produced text the document does not contain, so the verdict is **discarded rather than repaired** — an auditor shown a tidied-up version of a fabricated verdict is worse off than one shown nothing. Rounds carry `evidence_text` now, because a citation cannot be checked against a filename. `Assessment` gained `submission_id` alongside `check_instance_id` with a database CHECK that exactly one is set — the same two-parents shape Finding already has.
  **The adversarial document is exercised on the track where the incentive is strongest**: evidence on a finding is submitted by the party who benefits from acceptance, in answer to a specific accusation, by somebody who knows what the assessor is looking for. It never yields a recommendation to accept. And because the stub refusing an injection only demonstrates a well-behaved model, there is a test that rigs a model which obeys the injection completely — the citation check still throws the verdict out, because the quotation it invented is not in the document. Verified by planting: removing the citation check fails exactly those two tests.
  **Recurrence is unchanged at recall 50.0%, precision 75.0%** against the truth file's two planted groups, which is the point — swapping the taxonomy underneath it moved nothing. It still finds access-left-with-leavers across IT, Admin and HR over eleven months and still misses the supplier-files set entirely, which is the case a keyword stub cannot do. The demo found a real defect in itself here: it overrode a category on a finding inside a planted group before scoring, re-partitioned the group, and reported 16.7% recall for a detector achieving 50%. The override now runs after the scoring and on a finding outside the groups, with the reason in the code, because a demo that measures itself is worse than no demo.
  **The regenerated `results.md` then reported recall 16.7% where the seeded corpus gave 50%, and the gap was a real defect the taxonomy change exposed.** The recurrence candidate shortlist is capped at twelve and was capped on age alone — an arbitrary tiebreak the activity track wins on volume, since it produces hundreds of findings a year against the audit track's dozens. Twelve slots filled with system-generated descriptions and the auditor-written prior that *is* the recurrence never reached the model. It was invisible while the taxonomy was hardcoded, because activity findings piled into `control_not_performed`, a category the audit track barely used; deriving the categories redistributed them into `access_not_revoked` and the crowding surfaced. The shortlist now orders same-track-first, then oldest — same-track-*first* rather than only, because an auditor finding a gap a periodic check also caught is a real link, it just should not win a slot by weight of numbers. The shortlist and the measurement now agree about what is being compared. Full-pipeline recall is back to 50.0% / precision 75.0%, and a test buries an audit prior under three shortlists' worth of activity noise and asserts it survives.
  New `evaluation/demo_intelligence.py` walks all four capabilities and runs as a test. It sits in `evaluation/` rather than `sentinelops/demo/` because scoring recurrence means reading the truth file: the first draft lived in the package and tripped `test_truth_isolation.py`, which is that test doing exactly the job it exists for. `evaluation/` is the package outside `src/` precisely so it may hold the answers. The provider-boundary allow-list is now five named stages, each with its reason recorded; `review.py` additionally has a static test asserting it cannot close a finding or answer a round.
- Slice 15 (2026-09-15): section 8 in full, deterministic, and tested against the patterns slice 11 planted rather than only for internal consistency. Most of the figures existed since slice 10e; what this slice changed is what they are allowed to depend on, and whether they can see what the corpus deliberately put there.
  **The section 8 recurrence figure no longer depends on a model.** Section 8 defines a recurring finding as the same gap category in a different unit, project or period, which is a grouping over stored fields. Until now `recurring()` reported only the detector's `recurrence_of` links, so the section 8 number was a function of the stub's vocabulary and `results.md` carried a paragraph apologising for a category being over-represented. `recurring_categories` now computes the definition directly: a category recurs when two or more findings in it do not all sit in one unit and one quarter. It is reported for all findings and for the audit track alone, because audit findings carry an auditor's own words while activity-track descriptions are generated from a control title. On the corpus at today every one of the nine categories recurs across all 97 findings, which is accurate and says little; the audit track, six recurring categories holding 24 of 27 findings, is the view that says something. The detector's 117 links are still reported beside it, labelled as advisory, and a test clears every link and asserts the section 8 figure does not move.
  **The trend has a verdict, and two reads of it.** Least-squares slope of open findings against month across the window, flat within 0.25 a month, because at fifteen to eighteen open a drift of three a year is one busy audit; and the last quarter's mean against the quarter before, flat within one finding. They disagree on this corpus and both are printed: rising at +1.19 a month from 1 open in January 2026 to 17 today, falling over the last quarter at 19.0 against 21.7. The last point is cut at today rather than month end, since the rest of April has not happened. The trend window is read from the first finding rather than assumed to start the January before `as_of`.
  **The planted patterns are detected, and the tests read the truth file to prove it.** Every planted multi-attempt remediation whose check was scheduled appears in `multi_round_findings`; every finding with three or more rounds traces to a planted three-attempt series, and five of the seven realised series reach three rounds, the other two closing in two because the stub reads a near-miss second attempt as compliant, which is the assessor's measured accuracy and not the analytics'. Both audit-track recurrence groups land in a recurring category spanning units and quarters; access-left-with-leavers is held whole across three units and eleven months, while the supplier-files group holds two of three, the third having been classified into another category, which is a section 2 use 1 question scored in slice 14. The realised activity-track coordinate set recurs in one category too.
  New `demo/section_eight.py` prints all nine figures for the corpus at today and runs as a test; the meter is read either side of the analytics call, and reporting costs 0 model calls against the 476 the replay spent producing the state.
  **Found and deliberately not fixed here: S1 and S2 only ever schedule 2026.** `trigger._open_periods` calls `periods_for(control.frequency, policy.year)` with `year=2026` and no `through`, so no 2027 compliance activity is ever scheduled; the latest due date of any instance is 2027-01-15, although the corpus holds 256 truth rows for 2027 periods. `test_trigger.py` locks this in at 447 instances under a comment calling it eighteen months, and `score_gap_detection` scores only instances present in both run and truth, so the 2027 rows drop out of the recall figure unannounced. The consequences show directly in the section 8 printout: no compliance activity is ever due in the next thirty days, every open finding at today is past target, and the 2027 months of the trend carry no new activity-track findings. Patching S1 alone crashes S2, whose `period_ends` is built from the same single year, and the fix will move the corpus figures section 10 pins, so it is its own piece of work rather than a line in this slice. Analytics keeps reading scheduled instances rather than growing a second scheduler that would disagree with S1.
- Slice 15z (2026-09-15): the scheduler covers the whole corpus window, and the scorers stop discarding what they cannot place. A correctness fix; no new features.
  **The bug.** `trigger._open_periods` called `periods_for(control.frequency, policy.year)` with `SchedulePolicy.year = 2026` and no end year, so S1 scheduled twelve months of an eighteen-month corpus: 447 instances, latest due date 2027-01-15, and 206 obligations in 2027 never raised. The same single-year assumption sat in S1's state machine, in S2 (`prescreen.run(year=2026)`, which crashed with a bare `KeyError` the moment S1 was patched alone) and in remediation (`reassess(year=2026)`). Corpus generation mostly used the full window already, but looked the consistency pair and the truth file's exception expectations up in one year; and cycle lists, the dashboard's start date, the pack period and the pack filenames all pinned 2026 or 2027 by hand. A stale comment in `synth/generate.py` still said `SIMULATED_TODAY` sat *after* the window, which is where a vantage point of 30 September 2027 was being quoted from; it is 15 April 2027 and sits inside.
  **The fix.** The window is stated once, `calendar.CORPUS_WINDOW` (2026-01-01 to 2027-06-30), recorded in a one-row `schedule_window` table when the corpus is seeded, and read back by S1, S2 and remediation through `sentinelops.window` — the pipeline still never imports `synth/`. A database with no window refuses to schedule rather than guessing. `periods.Window` and `periods_in` replace the year parameter and refuse, rather than return, a period that runs past the window or a label that does not rebuild to its own dates; a lookup outside the window raises `PeriodOutsideWindow` naming the instance. Cycle dates in the harness and demos derive from the window; the pack covers the window start to the calendar's date and is named for that span.
  **Why the scorer skipped 2027, and the rest of the class.** Every scorer joined on what the *run* produced, so an obligation the run never scheduled could not appear in any join. `score_gap_detection` iterated run verdicts and skipped keys the truth file did not know — a test asserted it — and never visited truth rows without a verdict. `missed_checks` accepted `truth_rows` and never read them; its denominator was the instances S1 had created, which is how the headline read 0.0% missed over 446 while 2027 obligations had never been raised. `score_recurrence` silently dropped planted members with no finding. `first_verdicts`, `current_verdicts`, `verdict_consistency` and `time_to_detection` folded slice 14's round assessments into one `""` key that was then skipped. And the manual model had the mirror image: it counted obligations not yet due as due. All of it now either raises `UnscorableRows` or counts the row under a named exclusion the report prints; `missed_checks` takes its denominator from the truth file and the harness raises `UnscheduledObligations` rather than score a run that left a due obligation unscheduled. The first cut of that reported one missed check — an unfiled obligation due *on* the vantage point — so the boundary now matches S1's own overdue rule: a check is not missed on the day it falls due.
  **Tests.** `test_trigger.py` no longer counts: it asserts every applicable obligation in every opened period is raised or excused, that the period range runs from the window's first day to its last, that no scheduled period falls outside the window even long after it ends, that the latest due date belongs to the window's final period, and that a database with no window refuses to schedule. The other pins set under the 2026-only schedule — 447 in the pack, 446 considered by S2, 302 assessed, 91 superseded — now assert the reconciliation they stood in for. Three tests had been passing on luck or on the defect: the manual model's disagreement was asserted at seed 4242 and is a coin flip on one identical-evidence group (it holds on twelve of twenty seeds), so it is asserted across seeds; the planted multi-round checks set aside, and count, series whose check is not yet due, since a 2027-Q2 check has had one cycle and the loop takes one round per cycle; and the near-miss ceiling of three became a named clause, because a fourth near-miss is now judged — CORAL's 2027-Q1 leavers clause, which the stub reads as `partial`, not a pass.
  **Before and after, not retuned.** Instances 447 → 653. Open findings at the vantage point 17 → **32**; ageing 1/2/0/14 → 2/2/2/14; 20 of the 32 are past target. Activities due in the next 30 days 0 → **19**. Gap-detection recall 94.1% → 94.0%, precision 96.0% → 95.5%, now over 553 judged instances with 136 obligations counted as not yet judged. Missed checks 0/446 → 0/482, and the manual comparison is finally over the same set: 18.8% of 483 where it was 19.6% of 689. Tokens per cycle 257,477 → 308,702. The trend verdict turns from falling to rising over the last quarter. The open count now sits well outside the fifteen to eighteen the stakeholder described; that figure was his reality rather than a target, and the corpus was not adjusted to meet it.
  **Evidence filed before the obligation existed, fixed in the same slice.** Two remediation series were first noticed filing their attempts in December 2026 against 2027-Q2 periods that open in April 2027. A scan found it was 32 filings, not two: all 14 `stale` originals were filed before their period opened — two of them before the corpus window itself began, 2026 evidence lodged in 2025 — and the 18 remediations dated from them followed. The cause was S2's freshness rule measuring a document's age from the day it was filed, which left the generator only one way to make evidence stale: file it early. An auditor reading the pack would have seen evidence for an obligation that did not yet exist. Submissions and evidence now carry a `document_date` distinct from `submitted_at`; freshness reads the document date; stale evidence is an old document filed on time; the trail's `evidence_bound` entry records both dates. Nothing in the corpus is now filed before its period opens and no remediation predates what it answers, both asserted in `test_synth.py`.
  Verifying that exposed a second, hidden coupling. Remediation series shared one random stream, and a series stops drawing when its next attempt would land after the vantage point — so re-dating 14 stale series silently re-rolled the filing gaps and document text of 64 unrelated ones. Each series now draws from its own stream, seeded from the corpus seed and the original it answers, so a change to one cannot move another. That re-rolls remediation randomness once relative to the committed 15z corpus; originals are unchanged apart from the 14 stale filing dates. Measured against 15z: open findings at the vantage point 32 → **20** (13 no longer open, 11 of them on stale originals, plus one newly open, both of the non-stale moves attributable to the re-roll); ageing 2/2/0/6; activities due in the next 30 days still 19; missed checks 0 of 482. Recall moved from 94.0% to **93.8%**, and no verdict changed. (This entry first said recall was unchanged; slice 16z diffed the two runs instance by instance and corrected it.) Three stale originals were "filed" in 2026 against 2027 periods: `CHK-ACCESS-REVIEW-PRJ-ATLAS-2027-Q2`, `CHK-CONTROLLED-DOCS-PRJ-CORAL-2027-Q2` and `CHK-INTERNAL-AUDIT-READY-PRJ-BEACON-2027-H1`. The scheduler fix had scored all three as caught gaps. They are now filed on 2027-07-07, after the 2027-04-15 vantage point, so they have left the scored set: TP 63 → 60, FN 4 unchanged, unjudged 136 → 139. That is the date fix doing its job — before it, the pipeline was judging evidence for obligations that did not yet exist. Every other verdict and every evidence hash is identical between the two runs. By year: **94.1%** on 2026 periods (48 of 51), and **92.3%** on 2027 (12 of 13, where the scheduler fix alone read 15 of 16) — the figure the single-year scheduler had been hiding.
- Slice 16 (2026-09-15): the two generated artefacts — the prioritisation brief and the audit report — each checkable claim by claim, and both rendered as markdown and self-contained HTML.
  **The ranking is a documented formula.** New `priority.py`, which imports nothing from the provider boundary: `severity points x unit criticality + timing + recurrence links + follow-ups`, every weight a named constant and `formula_table()` printing them, the same reason section 5's policy table is printable. Each ranked row carries its components and a one-line `explain()` of the arithmetic. Severity is multiplied by criticality because a Major against a critical unit is a larger exposure, not a Major plus something; the other three terms add because they are evidence a finding is not being dealt with. A finding with no severity, a unit with no recognised criticality, or a recurrence link to a finding that does not exist is refused rather than scored as the mildest case. The old inline score used severity, days overdue, follow-ups and escalation, and had no criticality, no days *to* target and no recurrence.
  **The brief is strict JSON, and every claim cites.** Version 1 returned a paragraph and a list of ids, and any sentence containing a digit could go uncited. Version 2 returns `top_priorities` (a ranked finding and a one-line reason), `emerging_patterns` and `recommended_focus`, each claim listing the finding ids and named metrics it rests on. The metrics are slice 15's, under stable names in `analytics.named_metrics`. `validate_brief` rejects an uncited claim, an invented finding or metric, an id mentioned but not cited, a reason longer than a line, or a reply of the wrong shape, and one failure withholds the whole brief with its reasons on the trail. Top priorities are always shown in the ranking's order: the model picks and explains, it does not reorder the queue. One call per cycle; advisory; a test asserts no finding, instance or round moves.
  **The audit report.** Every sentence in the summary now cites finding ids, counts included; the old exemption for a sentence with a digit in it — "this audit raised 4 findings" — was exactly the sentence that goes wrong when a model miscounts, and a test had enshrined it. `issue` used to write "reviewed and confirmed by the auditor before issue" onto the trail whether or not anybody had; confirmation is now its own step, `confirm`, by the auditor who conducted the audit, against the version they read — regenerating clears it, and an issued report is not regenerated. A withheld summary used to vanish with no reason anywhere; the reasons now go on the trail. Recurrence links name the earlier finding's unit and date, and a dangling link is refused.
  **A withheld draft is counted, never quoted.** The first cut listed rejection reasons on the rendered page, and an existing test caught it: the reason for rejecting a fabricated citation quotes the fabricated citation, so printing it republished the claim that was withheld. Rendered artefacts now say a draft was withheld and how many problems it had; the reasons live on the trail. The dashboard's operator view still shows them.
  **Found on the way: recurrence detection paid twice.** `detect_recurrence` re-asked every unlinked finding on every run, including ones already asked about with nothing new since. The harness runs it once so it never showed; the dashboard runs it each tick, and the token cost of one cycle would have carried every earlier cycle's questions. A finding is now asked again only when a candidate it was never shown has appeared, and a test asserts a second run spends nothing.
  New `demo/artefacts.py` replays to the cycle before today, meters today's cycle on its own, writes the brief and the report as markdown and HTML to `data/artefacts/`, and confirms and issues the report. One cycle: 59 model calls and 46,375 tokens, of which the brief is 1 call and 2,206 tokens; the audit report is 2 calls and 1,837 tokens. The counts are read off response objects from `FakeModelClient` and priced at placeholder rates, so the shares are the durable figure, not the dollars. The stub's brief describes rows in their own terms; a real model says why they matter.
  Open for a decision at the time, and taken in slice 16z: with timing capped at 120 points for anything ninety days late and severity x criticality spanning 7.5 to 60, age dominates the ranking — a long-overdue Observation in a low-criticality unit outranks a Major in HR that is seven weeks late. That follows the documented weights; whether it should is the stakeholder's call.
- Slice 16z (2026-09-16): two corrections and one investigation, no new features.
  **The 0.2 points of recall were a consequence, not noise.** The 15z entry said the date fix left recall unchanged; it moved it from 94.0% to 93.8%. The two runs were diffed instance by instance, with every field of every first assessment. No verdict flipped. Three stale 2027 originals, each "filed" in 2026 against a period that had not yet opened, had been caught as gaps. The date fix moved their filing to 2027-07-07, after the vantage point, so they left the scored set. The 15z entry now says so. To rule out the other explanation, HEAD was run twice under different string-hash seeds, and the per-instance dumps were byte-identical. A new test runs the evaluation a second time in a separate interpreter with its own hash seed, and asserts results.md and every first verdict are identical. results.md loses its wall-clock "Generated" stamp — the one line that differed between runs, and the reason timestamp-only diffs kept being reverted before commits — and names the vantage point instead.
  **Severity gates the ranking.** `Findings are ordered by severity band (Major, then Minor, then Observation; a finding more than 365 days past its target counts one band higher), and within a band by points = criticality + timing + recurrence + follow-ups.` The first version multiplied severity by criticality and added timing, and timing ran to 120 while severity x criticality spanned 7.5 to 60; retuning weights would only have moved the crossover, so severity became a gate. The one cross-band adjustment is a year past target — a full annual audit cycle, against a section 5 escalation of a week — and it moves a finding one band at most, never above Major; inside its new band it competes on points. Criticality became points (30/20/10/0), the spread the old formula gave inside the Major band, so findings raised as Major keep their relative order. At 2027-04-15 one finding ages up: FND-IA-2026-H1-02, a Minor 377 days past target, now second. The Minor in HR 247 days late goes from 2nd to 11th, the Facilities Observation 202 days late from 4th to 17th, and the HR Major 48 days late from 7th to 4th. Tests pin the gate against the worst case — every point a lower band can carry, a year late to the day, still ranks below the mildest finding of the band above — and the original inversion by name.
  **The brief's input, not its output.** Version 2 gave the brief the top twelve rows and portfolio-wide figures only, so "the access not revoked category appears 4 times among them" could only cite `recurring_category:access_not_revoked`, which counts 24 findings across the window. The citation resolved and the claim was still wrong, and a real model would pair them the same way. Version 3 adds counts over exactly the rows supplied — `listed_findings`, `listed_in_unit:*`, `listed_category:*`, `listed_band:*`, `listed_past_target`, `listed_with_recurrence_links` — in their own labelled block, and the system prompt requires a number to be cited from a metric of its own scope. The stub's prose is untouched; its two metric lookups now read the listed names, which is the choice a model makes once they are there. It now cites `listed_category:access_not_revoked = 4` and `listed_in_unit:IT = 6`. A test asserts every number in a statement is the value of a metric that statement cites. The stub's `change_not_authorised` label on an unrotated shared password is a FakeModelClient artefact and is left alone.
- Slice 16z, second pass (2026-09-16): the dates investigated, and the band promotion taken back out.
  **The vantage point and the window agree.** `SIMULATED_TODAY` is 2027-04-15 and lies inside `CORPUS_WINDOW`, 2026-01-01 to 2027-06-30. The vantage point was 2027-03-31 in slice 2 and 2027-09-30 in slice 10, after the window; slice 11 (`b5f4b55`) moved it inside, and the slice 10f entry above still shows the value of its day.
  **Dates outside the window, and why.** Every date the corpus generates, and every date-valued column after a full run to the vantage point, was scanned. Outside the window there are three kinds:
  - 102 original filings for the last periods (2027-Q2, 2027-H1, 2027-06), filed 2027-07-01 to 07-14 in their grace days, and their 97 document dates;
  - 84 check due dates of 2027-07-15, since a due date is the period end plus grace;
  - 3 document dates in 2025, all on stale evidence filed inside the window.

  Everything that happened, and everything planned, is inside. The window bounds obligation periods, not every date, and the new `tests/test_dates.py` says exactly that: periods inside the window; what happened inside it and by the vantage point; what is planned inside it; only the last periods' filings and due dates past its end, never past the last due date (2027-07-15); a pre-window document date only on stale evidence; nothing filed after the vantage point used. Every date column must be classified, or the test fails. Each rule was checked by planting a violation. The 138 filings dated after the vantage point sit in the inbox, skipped by S1, S2 and remediation. `token_usage.ts` is wall-clock (`datetime.now()` in the meter). That is outside section 12's rule, which names audit events and notifications; it is classified as not business time, and left.
  **No band promotion.** Promoting a finding more than a year past target moved a Minor above a Major on age alone, re-litigating a severity the auditor assigned and communicated in the audit report. Putting a promoted finding at the bottom of the band above instead would be a no-op, since it already heads its own band. So there is none: `Findings are ordered by severity band (Major, then Minor, then Observation), and within a band by points = criticality + timing + recurrence + follow-ups.` Age past a year is `analytics.CHRONIC_DAYS`: each ranked row carries a `chronic` flag, the rendered rankings mark it, and PA/InfoSec see a separate "Chronic findings" alert on the dashboard, listing them oldest first. Neither moves a row. At 2027-04-15 the ten ranked first are the nine open Majors and FND-FINDING-CLOSURE-HR-2026-Q2, the Minor in HR 247 days late. FND-IA-2026-H1-02, the Minor 377 days late, is back in the Minor band at 11th, and is the one chronic finding.
- Slice 17 (2026-09-16): the dashboard rebuilt as a compliance team's morning screen. Still Streamlit, and still one rule: layout in the pages, computation in `view.py`, actions in `service.py`, neither importing Streamlit.
  **Pages, scoped by role.** Native multi-page navigation (`st.navigation`), with the pages registered per role from one table, `view.PAGES`:
  - PA/InfoSec land on **Today**, which holds the review queue, the chronic findings alert, the overdue queue and the prioritisation brief. Their other pages are Findings, Schedule, Inbox, Portfolio, Recurrence, Audit trail and the Walkthrough.
  - Unit owners land on **My findings**, and also have Finding detail and Inbox.
  - Management get **Overview**.

  `app.py` is now the entry point only: theme, the header every page shares, the identity selector, the simulated calendar and meter, then navigation. Each page is a script in `ui/screens/`, and none of them defines a function. `shell.py` holds what several pages draw: the finding detail panel, the brief panel and the document frame. The identity selector changes both the pages and the permitted actions. The header always names who you are acting as, with their role and unit.
  **Absent, not hidden.** Close, Accept and Mark insufficient are rendered only for an identity that may use them. A test sets up an open finding with a round waiting, and confirms PA/InfoSec see all three controls. It then walks every unit owner page and asserts none of the three is in the element tree Streamlit sends the browser. It also asserts an owner cannot open an auditor's page by name, since the page is not registered for their role, and that the stylesheet hides nothing.
  **New actions:**
  - owners file an evidence round against their own unit's finding; the unit is checked in the service, as well as the role in the stage
  - PA/InfoSec accept a round, which closes the finding; the separation check runs before the round is answered, so a refusal cannot leave a round accepted with its finding still open
  - PA/InfoSec mark a round insufficient, which counts the chase
  - owners record progress
  - recipients mark notifications read

  An advisory reading is attached to each new round for the auditor.
  **The design system**, in `view.py`:
  - one accent (teal), one neutral scale, and three severity colours that nothing else borrows
  - every badge carries its label, and severity renders in tables as a labelled pill column
  - explicit heading and base font sizes, and tabular figures
  - a few large figures with a comparison at the top of each page
  - card panels; dates as 15 Apr 2027, in simulated business time
  - an empty state on every panel that says what would be there and why it is not; spinners on every slow action

  A test checks every colour in the stylesheet is in the palette.
  **Found by looking at it.** Every page was screenshotted headlessly at the vantage point for all three roles. Four fixes came of that:
  - The landing page opened scrolled halfway down. The document frame's `scrollIntoView` scrolls every ancestor, and a same-origin frame's ancestors include the dashboard. It now scrolls its own window, and the test that required `scrollIntoView` now forbids it. `components.v1.html` is deprecated, and the frame moved to `st.iframe`. The evidence inside is escaped, as before.
  - The owner could see the model's advisory reading of their own round. That reading is the auditor's: the owner benefits from acceptance and could write the next round to the model. It is now shown to PA/InfoSec only, and a test covers both sides.
  - Owner detail opened on a closed finding; it now opens on open work first.
  - `ID-ASSESSOR` appeared as a person; system identities are now named.

  **Tests moved with the panels.** The existing AppTest coverage checks the same behaviours: the dashboard renders, a cycle runs, upload reports back, an empty upload is refused, the chain verifies, and the walkthrough renders emphasis. Each is now checked on the page it lives on, reached through the identity selector and `switch_page`. My findings is filtered by unit in the repository query, and a spy on `Repository.list` asserts it.
  **Open, not changed:**
  - The seeded audit programme exists from the calendar's first day. At the demo's start date, Findings shows findings raised months "later", and Portfolio counts them. That was already true of the old screen, and it reads oddly at 28 Jan 2026; the demo reads best replayed to the vantage point.
  - Management have only Overview, as specified, although escalation notifications are addressed to managers.
  - On Findings, "band" is read as the ageing band, because since slice 16z a severity band is the severity.

  **Density pass, same slice.** The first cut read well but left a lot of empty space. Changes:
  - The main column had a 1320px cap and sat centred, so at 1920px a third of the screen was margin. It now uses the full width.
  - The type scale is set explicitly in `view.TYPE_SCALE`: 24px page title, 16px section, 14px body, 28px figures. Card, figure, badge and header spacing are tightened to match.
  - Finding facts sit two to a row.
  - Tables with a handful of rows size to them.
  - On Today, the review queue puts each item's details beside its evidence rather than above it. The right column, which ended early, now also carries Coming up (audits and activities in the next seven days) and Latest notifications, each linking to its full page.
  - Schedule's empty left column gains a due-by-unit chart.

  Screenshots at 1440px and 1920px caught more:
  - sidebar cards overlapping, from Streamlit's negative markdown margin
  - review buttons truncated at laptop width; they are now stacked
  - a clipped Late column
  - "1 activity / 2 activitys"

  **Sidebar badges and an icon rail.** Each sidebar page with something waiting for the acting identity carries a count, from `view.nav_badges`:
  - for PA/InfoSec, rounds awaiting review on Today, findings past target on Findings, work due this week on Schedule, and unread messages on Inbox
  - for an owner, overdue findings on My findings, rounds returned insufficient on Finding detail, and unread messages on Inbox

  Only counts that ask something of the person, capped at 99+. Streamlit's navigation renders page names as text, so each badge is a pseudo-element keyed on the link's address. Only digits ever reach that stylesheet.

  Collapsing the sidebar now leaves a 64px rail of page icons, each with its badge, instead of hiding it entirely. The stylesheet's only hiding rules are the collapsed rail's, and the test that controls are absent rather than hidden now checks exactly that.

  **Notifications open in a modal.** On the inbox, a row shows a pointer on hover, and a click anywhere on it opens the notification in `st.dialog`. The page behind is darkened and shows a not-allowed cursor.
  - The dialog is not dismissible from outside. Close, or Mark as read, is the way out.
  - Both reset the table's selection. A selection that survived the rerun reopened the same notification: the table is keyed on the filter and a counter the dialog bumps.
  - Checked in a browser: the pointer on hover, a cell click opening the modal, an outside click leaving it open, and Close keeping it closed.

  **Model spend moved to `/cost`.** Calls, tokens and cost came off the sidebar, the Audit trail figures and the brief's caption, which now reads without a call count. They moved to a page registered for every role with `visibility="hidden"`: in nobody's navigation, linked from no page, and reached only by typing the address. Spend is for whoever runs the system, not part of a compliance team's working screen. The page shows:
  - the totals
  - spend by what made the call (evidence assessment, recurrence, classification, taxonomy, review, brief, report), with each one's share of tokens
  - spend by tier and model

  Tests assert the landing page shows no spend, that `/cost` renders, and that no layout file links to it. The Walkthrough's step 4, "Count what that cost", still quotes the meter: it is a scripted demo step, and was left for a decision.

  Dates past the simulated day are also kept out of the inbox. The seeded audit programme writes its closure notices ahead of the calendar, so at the demo's start the inbox opened on next year's messages.
- Slice 18 (2026-09-16): the simulated calendar jumps to what matters, the scenario resets exactly, and simulated time is asserted everywhere.
  **Jump to next event.** New `timeline.py` lists each open finding's milestones: its first reminder, its target date, and the first day each escalation level is earned. It also lists planned audits. The sidebar names the earliest one — what, whose, when, and how many more that day — above a **Jump to next event** button. The button runs that day's cycle, and the named event happens on it. Every reminder is not a milestone: inside the reminder window the chase reminds on every cycle, so "the next reminder" is always tomorrow, and a jump to it would be a fixed step in disguise. The planner reads the engine's own rule. `followup._run` held its reminder and escalation arithmetic inline; it now calls `reminder_due` and `escalation_level_earned`, and the planner's dates come from `first_reminder_on` and `escalation_on`, each defined as the first day those functions change. A test pins the named rule to the arithmetic it replaced, over every severity and 100 days either side of target. `python -m sentinelops.demo.jumps` walks FND-CHANGED-PROCESS-IT-2027-03, a Major in IT, from 15 Apr 2027:
  - a first reminder on 17 Apr
  - target reached on 18 Apr, with a second chase
  - escalation level 1 to M. Castellanos on 19 Apr
  - escalation level 2 to H. Lindqvist on 20 Apr

  Every reminder and escalation lands on the trail, and every notification is dated on its simulated day. A test does the same on a constructed Minor finding: 13, 16, 21 and 26 Mar, gaps of 3, 5 and 5 days.
  **Scenario reset.** Start over became **Reset scenario**. The seeded state is built once into a snapshot beside the demo database, named for the corpus fingerprint and the schema hash so a change to either builds a new one. The reset is a byte copy of it, and the dashboard's first open starts from the same file. `service.state_digest` hashes every row of every table. In the demo, the seeded digest `50e0eb7b…` (0 checks, 5 open findings, 83 audit events, 66 notifications) became `af790d29…` after three jumps (203 checks, 1,037 events), and returned to `50e0eb7b…` on reset. Seeding is also asserted deterministic, and the snapshot equals a fresh in-memory seed.
  **Simulated time everywhere.** `tests/test_simulated_time.py` runs a stretch that ends before the real date: the scheduler, the calendar jump, then every action the dashboard offers.
  - rounds opened, found insufficient and accepted
  - a closure, a notification marked read, and an upload and re-assessment
  - the brief, and an audit report generated, confirmed and issued
  - the audit pack

  It asserts that no audit event and no notification stamp written during the run falls outside the stretch; a wall-clock stamp would be today's real date, after it. It found no leak. A second test appends an entry outside the clock and confirms the check catches it.
- Slice 19 (2026-09-18): the pipeline ran against a real provider. The run completed, but its accuracy cannot be measured, and further spending was stopped at the owner's decision.
  **A model per stage, as config.** `llm/models.py` maps each metered stage to a model and its published price: gpt-4.1-mini for assessment, classification, taxonomy and recurrence, and gpt-4.1 for the brief and the audit report. `SENTINELOPS_MODEL_<STAGE>` changes one stage without touching code, and the meter prices from the same table. The key is read from `.env`, which is gitignored.
  **Spend, all of it.** About $1.40 of the $10 cap, every line recorded in `data/real/spend.json`:
  - $0.776 in the saved run: 1,386 calls (assessment 906, recurrence 420, triage 60). The metered rows and the budget's own count agree exactly.
  - At least $0.6045 lost in two runs that saved nothing. The first died on an exception its handler did not catch, with the database in memory. A resume was killed when its session ended, and section eight checkpointed only on finishing.
  - $0.0124 on 26 naive-baseline calls, stopped once the defect below was found.
  - $0.0048 on a smoke test and prompt probes.
  **Why nothing can be quoted.** Every one of the 453 assessments the model judged was refused as unreadable. `assessment_v2` names three of the seven fields its schema requires, and the schema is validated locally but never sent to the provider. gpt-4.1-mini answered each criterion in its own layout, with quotations copied verbatim, and the pipeline refused the answers rather than guess at them. The refusal is the design working; the prompt is the defect. `FakeModelClient` always answers in the canonical shape, so no stub run could have found it. Everything downstream inherits it: the refusals raised 439 findings where the stub raised 113, so recurrence and severity agreement ran over the wrong population. `results.md` has a new section, generated from the run's files, saying which figures survive:
  - the taxonomy: 6 categories against the stub's 9, derived from audit descriptions the defect never touched
  - the metering reconciliation
  - the chain: 14,869 events verified

  The pack builds from that run's audit log alone and verifies. It was not written over the tracked pack, which still comes from the stub run.
  **Found and fixed:**
  - Triage told the model to answer "keyed by its id", and it did. `triage_v2` shows the exact shape; probed against the model at batch sizes 3 and 1.
  - Recurrence never named its `recurrences` wrapper. `recurrence_v2` does; probed.
  - Recurrence's flat 500-token ceiling could not hold an answer about a full 12-finding shortlist, and real shortlists average 11.4. It is now sized to the shortlist.
  - A triage batch returned with findings missing is halved and asked again, down to one finding. A finding unanswered on its own still refuses.
  - Section eight's stages built their own clients, which would have spent outside the budget; they are now handed the metered one. During a paid run the provider factory points at a provider that does not exist, so any fallback fails instead of spending.
  - Paid runs checkpoint every 25 calls and save on any exception, and `resume` finishes a stopped run without repaying for it. The baseline journals each paid answer, so an interrupted baseline resumes free.

  New tests cover the budget stop, the checkpoints, the journal, the reconciliation, baseline sampling and its cache key, the section-eight client, the split retry, the recurrence token ceiling, and that every prompt names the wrapper key its schema requires.
  **Open, not changed:**
  - The assessment prompt. The fix is the one triage and recurrence got. It was not made, because it could not be checked against the model without spending.
  - There is no real naive baseline, brief or audit report. The brief and report prompts have never been sent to a real model.
  - The API key was pasted into the conversation and should be rotated.
