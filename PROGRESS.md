# Progress

- Slice 1 (2026-08-27): project skeleton — pyproject, the 8 section-3 entities as dataclasses on SQLite behind a plain-sqlite3 repository layer, append-only AuditLog, isolated `llm/` boundary (protocol, fake + stubbed openai providers, factory, TokenMeter, versioned prompts), hardcoded fixtures and console report in `demo/`, and one end-to-end path in `main.py`. 31 tests passing.
- Slice 2 (2026-08-27): seeded synthetic corpus — 7 process areas, 14 clause-structured controls (3 structured), 12-month calendar, 316 evidence submissions across all seven qualities incl. near-misses, a two-area consistency pair, 3 exceptions (one lapsing mid-year), 6 remediations, and a truth file outside `src/` that nothing in the package may read. 125 tests passing.
- Slice 3 (2026-08-28): S0 applicability rules engine — deterministic `applies_when` evaluation (equality, any-of list, empty = everywhere) producing the control set per area with a per-condition explanation, unknown-attribute validation, one audit event per area, and zero model calls asserted with every provider rigged to explode. Reproduces all 64 corpus pairings. 154 tests passing.
- Slice 4 (2026-08-28): S1 scheduling and trigger — `run_cycle(as_of)` as the single entry point (cron, UI and CLI share it) with `advance_calendar` delegating to it, 343 instances generated across the 12-month calendar keyed on control x area x period, pending/submitted/overdue/waived/assessed state machine, escalation up a 3-level owner chain read back out of the audit trail, notifications logged and never sent, exception expiry alerts with lapse-stable suppression, human-vs-system trigger recorded. Period arithmetic moved from synth/ to `periods.py` so the engine does not import test data. 201 tests passing.
- Slice 4a (2026-08-28): added EXC-004, a fourth exception granted 2026-05-11 after CTRL-CRYPTO-KEY/AREA-HR 2026-Q1 was already open and overdue, making the `waived` transition reachable from the corpus. Introduced `waives()` in S1 as the complement of `covers()`: suppression settles at generation, waiver settles every cycle. Its window contains no period end, so the corpus fingerprint is unchanged at 8ef5f63c47512bdc. 213 tests passing.
- Slice 5 (2026-08-28): S2 pre-screen — five ordered rules (no evidence, wrong type, unchanged content hash carried forward, stale by freshness, structured threshold evaluated in code) resolving 140 of 342 instances (40.9%) with zero model calls; only 202 ambiguous document instances survive to S3. Evidence made write-once at both layers: `WriteOnceRepository` has no update/delete and SQLite triggers refuse UPDATE and DELETE. Findings gain `decided_by` and `carried_forward_from`. 248 tests passing.
- Slice 6 (2026-08-28): S3 assessment — paragraph chunking with deterministic term-overlap retrieval, one metered model call per surviving instance (202 of 342), strict JSON with enum-validated verdict, citations verified against the source or the assessment fails to insufficient_evidence + human review, untrusted-evidence delimiters with an adversarial document in the corpus, provenance (criteria hash, prompt version, evidence hash) on every Finding, retry-once then fail cleanly, confidence floor 0.6. Real OpenAI provider implemented, key from .env via env var. 294 tests passing.
- Slice 7 (2026-08-28): S4 flagging and the resolution loop — three distinct categories in a `Flag` table (121 gap, 32 overdue, 4 exception), severity stamped from a documented four-term formula with its arithmetic in the rationale, 153 Actions raised and routed to owning teams with SLA by severity band, Actions escalating on their own timer separate from the check, and `reassess(check_instance_id)` closing the loop on demand: remediation bound as new write-once evidence, re-run through S2/S3, new Finding with supersedes_finding_id, Action resolved with a note. All 6 corpus remediations close. 343 tests passing.
- Slice 7b (2026-09-02): audit log made tamper-evident — every AuditEvent carries a monotonic `seq`, the `prev_hash` of the entry before it, and its own `entry_hash` covering that link. `verify_chain()` walks the log and returns OK or the sequence number of the first broken link, distinguishing an altered entry, a broken predecessor link and a non-contiguous sequence. 2397 entries verify clean over a full year; one owner rewritten by raw UPDATE at seq 1500 is caught with 1499 entries verified before it. No other behaviour changed. 360 tests passing.
- Slice 8 (2026-09-02): evaluation harness in a top-level `evaluation/` package (outside `src/`, so the truth-isolation guarantee is structural) — competent naive baseline that skips empty instances and caches by document hash but has no applicability, pre-screen or retrieval, disk-cached and never recomputed; a documented simulated-manual model with a sensitivity table; all seven metrics; `results.md` written with every scope qualifier in the file. Pipeline run month by month so time-to-detection is real. 389 tests passing.
- Slice 8b (2026-09-02): Audit Evidence Pack generator (`pack.py`) built from the audit log alone — cover, coverage, exception register, findings register with cited excerpts inline, action register, full chronological trail, method note and chain verification, rendered to markdown and self-contained HTML. Enforced two ways: a static check that no live-state table is queried and no repository imported, and a runtime test building a complete pack from a database containing only the audit log. Building it exposed three holes in the trail, all fixed at source: findings stored a citation count not the text, exceptions that never lapsed produced no event, and instances recorded ids but no titles. 413 tests passing.
- Slice 8c (2026-09-02): fixed two defects the audit pack exposed — a waiver granted after S2 had already closed a check as failed now excuses it (instance to waived, gap/overdue flag closed, action resolved with a note, finding left standing on the record), and audit entries are stamped from a simulated clock scoped to each stage entry point so the trail carries cycle dates rather than the wall clock of a machine replaying a year in seconds. 422 tests passing.
- Slice 9 (2026-09-02): Streamlit dashboard — one screen with status by area, overdue/escalation queue, finding detail with cited spans highlighted inside the source document, open actions, per-check audit timeline, live token and cost meter, calendar advance, run-cycle, re-assess, evidence upload, audit-pack generation and chain verification. Logic split into `view.py` and `service.py` (neither imports Streamlit) with `app.py` as layout; headless AppTest runs the real script and clicks the buttons. 458 tests passing.
- Slice 9a (2026-09-02): dashboard usability — guided six-step walkthrough in plain English driving the same service functions as the console, evidence uploads and re-assessment now report back on screen (messages parked in session state, since anything drawn before `st.rerun()` is discarded), long documents rendered in a fixed-height scrolling iframe that opens on the first citation, and markdown emphasis converted for styled panels. Plus `docs/USER_GUIDE.md` with 13 screenshots captured from the running app by `docs/capture.py`. 488 tests passing.
- Slice 10a (2026-09-02): v3 vocabulary — `ProcessArea` -> `AuditableUnit` with a `kind`, new `Identity` with roles and reporting lines, `AuditEvent.actor` -> `actor_kind` + `actor_identity`, and the v2 `Finding` (an AI verdict) -> `Assessment`, which vacates the name before a later slice reuses it for the central tracked object. Escalation walks `reports_to` through a new `directory` rather than gluing "Head of" onto a team name. Unit roster deliberately unchanged (7 units, 64 pairings, 343 instances) so every moved test is a rename, not a recount. 488 tests passing, chain verifies clean over 2,212 entries.
- Slice 10b (2026-09-07): the v3 `Finding` becomes the central tracked object. `Action` and its seven-state machine are gone; a finding is **Open or Closed** and only a `pa_infosec` identity closes one, with remarks required. S4 raises the finding and stops — the computed severity band becomes a *suggestion* and the auditor's assignment is recorded beside it, so an override stays visible. New `stages/followup.py` runs the two section-5 clocks on every open finding: reminders and escalation, both severity-driven, escalating up `reports_to` *and* to PA/InfoSec, with a test asserting no row in the table lets anything drift more than a week past target and another asserting reminders continue after escalation. `follow_up_count` increments on every reminder and every insufficient-evidence round; owner progress is self-reported and closes nothing. Remediation now records progress, then either closes the finding or counts the failed round and leaves it open. The audit pack's action register becomes a finding register reconstructed from the trail — reminders, escalations and rounds included. Two defects found on the way: level-1 escalation and the follow-up stage writing wall-clock timestamps, both fixed and both now tested.
- Slice 10c (2026-09-07): section 4's review loop and section 7's segregation of duties. `EvidenceSubmission` vacated to `InboundSubmission` (the pre-binding inbox it always was) and reused for what section 3 says it is: one review round on one finding, with `auditor_response`, never edited once answered — revised evidence opens round N+1. New `authority.py` holds two separate rules, because conflating them is how segregation of duties quietly stops working: the role table, and the rule a table cannot express, that whoever submitted evidence may never accept it or close the finding. Both blocked paths tested, and both guards verified by removing them and watching exactly five tests fail. UI gained an identity selector; the owner's Close control is absent, not disabled.
- Slice 10d (2026-09-07): the audit track. `ScheduledAudit` with the four kinds the stakeholder named, findings raised under an audit carrying `audit_id` and `source="audit"`, and a test asserting every finding has exactly one parent — never both, never neither. The section 6 report is deterministic except one paragraph, and every statement in that paragraph must cite finding ids, verified rather than trusted: a summary citing a finding the audit never raised is withheld and the report issues without it. The "only S3 may reach a model" test caught `audits.py` immediately; the allow-list was widened to two named stages with the reason recorded, plus a test asserting it is exactly those two.
- Slice 10f-corpus (2026-09-07): the section 10 reshape. Eighteen months (2026-01 to 2027-06, vantage point 2027-09-30), eleven units — six support functions, three project teams, two departments — and a corpus that produces **83 findings with 17 open at today**, against 214 and 211 before. The old quality weights had half of everything failing, which is not a programme in trouble but a corpus built to make a detector look good; the new ones fail about one in six. Multi-round remediations plant fixes that do not work first time, so eight findings take two rounds and seven take three and `follow_up_count` measures something real. Eight audits across all four kinds with seventeen auditor-raised findings, and two recurring gap sets each spanning three units and three periods, both crossing the support-function / project-team line — the comparison a functional org chart makes hardest to see, and the thing slice 10e's recurrence detector needs to have something to find. Seeding deliberately does not generate audit reports: drafting is the audit track's one paid step and a fixture that runs hundreds of times must not carry a bill. Three real defects surfaced during the repair rather than count updates: the audit pack showed no severity at all for audit-raised findings because it only read the model's suggestion; `periods_for(freq, year)` silently found nothing once the corpus crossed a year boundary, now fixed by `period_from_label`; and gap detection was scored on the *current* verdict, so every successfully remediated gap counted as a missed one. A fourth was the most instructive — the truth file on disk went stale mid-reshape and the evaluation scored a fresh corpus against old ground truth, reporting 12.8% recall for a pipeline that had caught almost everything. `evaluation.harness` now refuses to score on a fingerprint mismatch, because a plausible wrong number is worse than an obviously broken one.
- Slice 10e (2026-09-08): section 2's remaining model uses, in one `stages/intelligence.py` because they share a shape — they read *findings* rather than evidence, and none of them may change a status. **Classification** maps an auditor's free text onto a closed ten-category taxonomy; the taxonomy is ours and the module says so, because the stakeholder said no taxonomy exists and inventing one and then pretending it was theirs would be dishonest. A free-form label was rejected on purpose: recurrence built on it would measure the model's vocabulary rather than the organisation's problems. **Recurrence** does the elimination deterministically — same category, different unit or period, raised earlier — and asks the model only the part a rule cannot do, which is whether two descriptions written months apart by different auditors describe the same failure. Twelve of seventeen findings need no call at all. On the corpus it links privileged access left with leavers in Payments (March 2026) to contractor accounts left active in IT Services (November) to a partner API credential never revoked in Payments again (February 2027): three units, eleven months, no shared vocabulary. **The brief** is one call per cycle over a ranking the code computed, because a model that ordered the queue would be prioritising, which section 2 does not permit. Every claim in it cites finding ids or it is withheld, the same rule as the audit report. Both guards verified by removal. New `analytics.py` carries all nine of section 8's figures with a test asserting it imports no provider and a stronger one running it with `get_client` rigged to explode; the allow-list of stages that may spend is now three, named, with a companion test naming the never-AI stages so moving a decision into a spending module has to get past a test that knows the difference.
- Slice 10f (2026-09-08): the surfaces. `analytics.py` renders into the dashboard behind a **Portfolio analytics** panel and into `results.md` as a section 8 block; both say plainly that no model is involved, and the cost meter does not move when the panel opens. Two new panels carry section 2's advisory uses — recurrence links with the earlier finding quoted next to the new one, and the prioritisation brief behind a button, because one call per cycle should be a call somebody asked for. Screenshots re-captured against the live app; the capture script now polls for each button instead of sleeping a fixed 2.5 seconds and presses **Start over** first, which fixes two real defects in it: a cycle over five hundred instances takes longer than the old wait, so the shutter fired on a page that had not advanced, and the demo database persisted between runs, so a second capture photographed a walkthrough somebody had already finished.
  Two defects found while wiring the surfaces. Activity-track findings were described by the assessment's rationale, which is generated from a handful of phrasings — eighty findings shared four sentences, so anything comparing findings was comparing boilerplate and recurrence matched everything to everything. They now name the control, the period and the specific gap. And `analytics.recurring` merged links transitively, so A→B and B→C became one "set"; on this corpus that collapsed into a single set of forty-five findings across ten units, which is the union-find algorithm reporting that the graph is connected rather than anything a team can act on. It now reports links plus a per-category rollup, with the few short chains that genuinely read as one story.
- Slice 11 (2026-09-08): the corpus rebuilt at the stakeholder's own volume and in their own vocabulary. Six support functions named as they named them — IT, Admin, Purchase, HR, Finance, Facilities — plus four project teams, two auditors and two managers on reporting lines that are not uniform (Finance and Purchase report to the director, everyone else through operations, so escalation from Finance reaches a different person at level one than escalation from IT). The control library is now section 3's fourteen activities and nothing else; the six we had invented — access-export completeness, encryption key rotation, complaint-handling SLAs — were plausible and were not this organisation's, which made every applicability count a statement about a company that does not exist.
  **"Today" moved inside the window**, to 15 April 2027. That is the change with the largest visible effect: with the vantage point past the end, every obligation had already fallen due, every open finding was months overdue and the ageing buckets collapsed into a single 90+ column, which is not what a portfolio looks like on any real Tuesday. It now spreads 1 / 2 / 0 / 14 with an audit still ahead of today. 91 findings, 17 open, severity across all three, 5 findings needing three evidence rounds and 26 rounds the auditor rejected.
  **Recurrence is now scored rather than admired.** The truth file records which findings are *intended* to be the same gap, resolved to real ids, so the detector can be marked: recall 50%, precision 75% over six planted pairs. It finds the access-left-with-leavers set across IT, Admin and HR — eleven months, no shared vocabulary — and misses the supplier-files set entirely, which is exactly the case a keyword stub cannot do. Scored on the audit track only, because activity-track descriptions are generated by the system and "the same gap elsewhere" is derivable there by a rule; scored across both, precision read 1% and measured nothing.
  The stub was taught to read "3 of 5" shortfalls, because the new clause set states failures that way far more often and comparing two numbers is a mechanical job a rule can legitimately do — it was missing six near-misses instead of one. What survives is three instances of one clause, "actions arising are tracked only inside the closed tickets", which has no negation, no shortfall and no hedge: reading it as a failure requires understanding what was asked for. Measured at 96.4% agreement and recorded rather than tuned away.

