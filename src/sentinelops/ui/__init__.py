"""The dashboard.

    streamlit run src/sentinelops/ui/app.py

`view.py` computes what the screen shows and `service.py` does what the buttons
do; neither imports Streamlit, so both are tested like any other module. `app.py`
is layout over the two.
"""
